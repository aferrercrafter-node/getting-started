/*

    require('fs');

    readFile(path[, options])
    createReadStream(path[, options])

    writeFile(path[, options])
    createWriteStream(path[, options])

    appendFile(path, data[, options])
    copyFile(path, dest[, flags])
    stat(path[, options])
    access(path[, mode]), chmod(path[, mode]), chown(path[, mode])
    link(existingPath, newPath), unlink(path)
    truncate(path[, len])

    mkdir(path[, mode])
    readdir(path[, mode])
    rmdir(path)
    rename(oldpath, newPath)

*/